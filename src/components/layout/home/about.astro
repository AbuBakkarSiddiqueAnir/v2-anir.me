---
import config from "@/configurations/config";
type TechKey = "Language" | "Frontend" | "Backend" | "Database" | "CMS";
---
<script>
import { gsap } from "gsap";

import { createTimeline,fadeInAndSlideIn, fadeInAndSlideInFromX } from "@/helper/gsap";


    const tl1 = createTimeline("#about");

  const tl4 = createTimeline("#techs");

  tl1.add(fadeInAndSlideIn("#about-title-1", 100));
  tl1.add(fadeInAndSlideIn("#about-p-1", 150));
  tl1.add(fadeInAndSlideIn("#about-image-1", 250));

  gsap.utils.toArray('.tech-area').forEach((tech,index) => {
    tl4.add(
      fadeInAndSlideInFromX(tech as HTMLElement, 100 ,0.1*index)
    )
  })
</script>

<section id="about" class="main-section min-h-[90vh] w-full px-6">
  <div class="mx-auto h-auto max-w-[60rem] text-left">
    <h3 id="about-title-1" class="text-h-md">
      About me <span class="text-[10px] text-green"
        >______________________
      </span>
    </h3>

    <div class="content mt-10 flex max-w-full flex-col gap-8 md:flex-row">
      <div id="about-p-1" class="order-2 w-full md:order-1 md:w-3/5">
        <p  class="inter text-b-2 text-light-slate">
          Hello! My name is Abu Bakar Anir and I enjoy creating things that live
          on the internet. My interest in web development started back in 2020
          when I decided to build this with small web apllications like games ,
          websites with javascript, html, css
          <br />
          <br />
          Fast-forward to today, and I’ve had the privilege of working at a designing
          firm as software developer, a start-up, a remote based software agency.
          My main focus these days is building accessible, inclusive products and
          digital experiences for a variety of clients.
          <br />
          <br />
          And also, I have been developing products for local business owner for
          time to time. And it happens that those opportunities trigger my compassion
          for building things that serves solutions for real problems.
          <br />
          <br />
          Learning and trying new technologies never bores me. Here are a few technologies
          I’ve been working with recently:
        </p>

        <div id="techs" class="my-6 grid w-full grid-cols-3 md:w-2/3">
          {
            Object.keys(config.techs).map((key) => {
              return (
                <div  class="mt-3 tech-area">
                  <h5 class="mb-2 text-b-2">{key}</h5>
                  <ol>
                    {config.techs[key as TechKey].map((tech) => (
                      <li class="relative py-1 pl-4 font-mono text-b-3 text-slate">
                        {tech.title}
                      </li>
                    ))}
                  </ol>
                </div>
              );
            })
          }
        </div>
      </div>

      <div class="order-1 w-full md:order-2 md:w-2/5">
        <div
        id="about-image-1"
          class="group  relative max-w-[18rem] cursor-pointer overflow-visible"
        >
          <div
            class="absolute left-[3%] top-[3%] z-[-1] h-full w-full rounded-sm border border-solid border-green/40 transition-all duration-300 group-hover:left-[4%] group-hover:top-[4%] group-hover:border-dashed"
          >
          </div>
          <img
            class="z-50 aspect-square rounded-sm hue-rotate-[17deg] transition-all duration-300 group-hover:-translate-x-1 group-hover:-translate-y-1 group-hover:shadow-xl group-hover:hue-rotate-[0deg]"
            src="/anir.jpeg"
            alt="abu bakkar anir"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  li::before {
    content: ">";
    color: #64ffda;
    font-size: 12px;
    position: absolute;
    left: 0;
  }
</style>
